# ansible/site.yml
---
- name: Test playbook
  hosts: localhost
  connection: local
  tasks:
    - name: Visa ett meddelande
      debug:
        msg: "Ansible kör från kontrollnoden!"

    - name: Visa en variabel
      debug:
        msg: "Miljö: {{ my_environment }}"
      vars:
        my_environment: "lab"
